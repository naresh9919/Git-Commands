-> Git init (for create new empty repository)
it will create.git (hidden file )

-> git status 
-> git config --global user.name "Y Naresh babu"
-> git config --global user.Email "ynareshbabu1991@gmail.com"
-> git config --list 
-> git config --global --unset user.name (to unset user.name)
-> git config --global --unset user.email (to unset email)
